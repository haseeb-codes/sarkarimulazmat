generator client {
  provider = "prisma-client"
  output   = "../src/lib/server/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

/// Job Postings model matching the zero-inference extraction schema
model JobPosting {
  id                       String          @id @default(uuid())
  slug                     String          @unique
  active                   Boolean         @default(true)
  title                    String
  description              String?
  department               String
  projectProgramName       String?         @map("project_program_name")
  postedBy                 String?         @map("posted_by")
  employmentType           EmploymentType? @map("employment_type")
  grade                    String?
  equivalentGrade          Int?            @map("equivalent_grade")
  qualificationLevel       Int             @map("qualification_level")
  degrees                  String[]        @default([])
  degreeArea               String[]        @default([]) @map("degree_area")
  educationLevel           String          @map("education_level")
  educationYears           Int?            @map("education_years")
  certifications           String[]        @default([])
  donorName                String?         @map("donor_name")
  vacancies                Int?
  placeOfPosting           String[]        @default([]) @map("place_of_posting")
  domicile                 String?
  experience               String?
  preferredExperience      String?         @map("preferred_experience")
  minAge                   Int?            @map("min_age")
  maxAge                   Int?            @map("max_age")
  ageRelaxation            String?         @map("age_relaxation")
  gender                   String[]        @default([])
  isQuotaBasis             Boolean         @default(false) @map("is_quota_basis")
  maleQuota                String?         @map("male_quota")
  maleHeadcount            Int?            @map("male_headcount")
  femaleQuota              Boolean         @default(false) @map("female_quota")
  femaleHeadcount          Int?            @map("female_headcount")
  disabilityQuota          Boolean         @default(false) @map("disability_quota")
  minorityQuota            Boolean         @default(false) @map("minority_quota")
  nocRequired              Boolean         @default(false) @map("noc_required")
  applicationFee           String?         @map("application_fee")
  salary                   String?
  lastDateToApply          String?         @map("last_date_to_apply")
  daysToApply              Int?            @map("days_to_apply")
  applicationPostalAddress String?         @map("application_postal_address")
  applicationOnlineAddress String?         @map("application_online_address")
  newsSource               String?         @map("news_source")
  adCode                   String?         @map("ad_code")
  adDate                   String?         @map("ad_date")
  url                      String?
  email                    String?
  phone                    String?
  rawText                  String          @map("raw_text")
  notes                    String?
  createdAt                DateTime        @default(now()) @map("created_at")
  updatedAt                DateTime        @updatedAt @map("updated_at")

  @@index([active])
  @@index([department])
  @@index([postedBy])
  @@index([qualificationLevel])
  @@index([equivalentGrade])
  @@map("job_postings")
}

model JobPostings {
  row_id                              Int?
  slug                                String    @id
  active                              Boolean?
  title                               String?
  department                          String?
  collar                              String?
  is_army_officer_required            Int?
  is_women_only_job                   Int?
  is_transgender_only_job             Int?
  is_law_enforcement_job              Int?
  is_management_job                   Int?
  is_driver_job                       Int?
  is_nursing_job                      Int?
  is_allied_health_job                Int?
  is_bds_job                          Int?
  is_dpt_job                          Int?
  is_naib_qasid_job                   Int?
  is_cooking_job                      Int?
  is_computer_science_job             Int?
  is_information_technology_job       Int?
  is_llb_job                          Int?
  is_mbbs_job                         Int?
  is_pharmacist_job                   Int?
  is_teaching_job                     Int?
  is_education_degree_job             Int?
  is_civil_engineer_job               Int?
  is_mechanical_engineer_job          Int?
  is_electrical_engineer_job          Int?
  is_chemical_metallurgy_engineer_job Int?
  is_mechatronics_engineer_job        Int?
  is_aviation_aeronautics_job         Int?
  is_dae_job                          Int?
  is_mba_job                          Int?
  is_mcom_job                         Int?
  is_bcom_job                         Int?
  is_commerce_job                     Int?
  is_accounting_finance_job           Int?
  is_acca_job                         Int?
  is_acma_job                         Int?
  is_statistician_job                 Int?
  is_data_science_job                 Int?
  is_agriculture_job                  Int?
  is_environmental_sciences_job       Int?
  is_social_science_job               Int?
  is_psychology_job                   Int?
  is_islamic_studies_job              Int?
  is_communication_studies_job        Int?
  is_economist_job                    Int?
  qualification_level                 Int?
  degrees                             String?
  degree_area                         String?
  education_level                     String?
  education_years                     Int?
  certifications                      String?
  license_required                    String?
  place_of_posting                    String?
  domicile                            String?
  domicile_any                        Int?
  domicile_punjab                     Int?
  domicile_sindh                      Int?
  domicile_sindh_urban                Int?
  domicile_sindh_rural                Int?
  domicile_kpk                        Int?
  domicile_balochistan                Int?
  domicile_ict                        Int?
  domicile_ajk                        Int?
  domicile_gb                         Int?
  domicile_fata                       Int?
  experience                          Int?
  gender                              String?
  is_quota_basis                      Boolean?
  disability_quota                    Boolean?
  minority_quota                      Boolean?
  salary                              Int?
  salary_estimated                    Int?
  last_date_to_apply                  DateTime? @db.Date
  application_postal_address          String?
  source_file                         String?
  file_creation_date                  DateTime? @db.Date
  province                            String?
  file_path                           String?
  employment_type                     String?
  application_online_address          String?
  ad_code                             String?
  url                                 String?
  posted_by                           String?
  vacancies                           Int?
  application_fee                     String?
  challan_fee_amount                  Int?
  max_age                             Int?
  notes                               String?
  project_program_name                String?
  email                               String?
  grade                               String?
  grade_derived                       String?
  age_relaxation                      String?
  min_age                             Int?
  apply_within_days                   Int?
  donor_name                          String?
  news_source                         String?
  male_headcount                      Int?
  female_headcount                    Int?
  license_root                        Boolean?
  domiciled                           String?
  domiciled_any                       Int?
  domiciled_punjab                    Int?
  domiciled_sindh                     Int?
  domiciled_sindh_urban               Int?
  domiciled_sindh_rural               Int?
  domiciled_kpk                       Int?
  domiciled_balochistan               Int?
  domiciled_ict                       Int?
  domiciled_ajk                       Int?
  domiciled_gb                        Int?
  domiciled_fata                      Int?
  supabase_file_path                  String?
  ad_date                             DateTime? @db.Date
  url_web_title                       String?
  is_active                           Float?
  ad_slug                             String?
}

/// Curated SEO landing pages with preset filters (e.g. /engineering-jobs, /bs-cs)
model CategoryPage {
  id               String   @id @default(cuid())
  slug             String   @unique
  title            String
  h1               String
  meta_description String
  intro_content    String
  filters          Json
  is_indexed       Boolean  @default(true)
  created_at       DateTime @default(now())
  updated_at       DateTime @updatedAt

  @@map("category_pages")
}

/// Anonymous visitor identity (cookie-linked)
model Visitor {
  id          String        @id @default(uuid())
  first_seen  DateTime      @default(now()) @map("first_seen_at")
  last_seen   DateTime      @default(now()) @map("last_seen_at")
  visit_count Int           @default(1)
  user_id     String?       @map("user_id")
  ip_address  String?       @map("ip_address")
  page_views  PageView[]
  saved       SavedSearch[]
  search_logs SearchLog[]

  @@index([ip_address])
  @@map("visitors")
}

/// Filter usage log for aggregate analysis
model SearchLog {
  id           String   @id @default(cuid())
  visitor_id   String?  @map("visitor_id")
  user_id      String?  @map("user_id")
  filters      Json
  result_count Int      @map("result_count")
  created_at   DateTime @default(now()) @map("created_at")
  ip_address   String?  @map("ip_address")
  visitor      Visitor? @relation(fields: [visitor_id], references: [id])

  @@index([created_at])
  @@index([visitor_id])
  @@index([ip_address])
  @@map("search_logs")
}

/// Page view log — every link visited, tracked by IP
model PageView {
  id         String   @id @default(cuid())
  visitor_id String?  @map("visitor_id")
  ip_address String?  @map("ip_address")
  path       String
  query      String?
  referrer   String?
  created_at DateTime @default(now()) @map("created_at")
  visitor    Visitor? @relation(fields: [visitor_id], references: [id])

  @@index([created_at])
  @@index([ip_address])
  @@index([visitor_id])
  @@index([path])
  @@map("page_views")
}

/// Contact form submissions from /contact
model Email {
  id         String   @id @default(cuid())
  name       String
  email      String?
  message    String
  created_at DateTime @default(now()) @map("created_at")
  contact    String

  @@index([created_at])
  @@index([email])
  @@map("emails")
}

/// Saved filter sets for anonymous visitors and signed-in users
model SavedSearch {
  id               String   @id @default(cuid())
  visitor_id       String?  @map("visitor_id")
  user_id          String?  @map("user_id")
  label            String?
  filters          Json
  email_subscribed Boolean  @default(false) @map("email_subscribed")
  created_at       DateTime @default(now()) @map("created_at")
  visitor          Visitor? @relation(fields: [visitor_id], references: [id])

  @@index([visitor_id])
  @@index([user_id])
  @@map("saved_searches")
}

/// Newspaper job advertisements (grouped postings by `ad_code`).
model Ads {
  ad_slug              String    @id
  ad_code              String?
  ad_headline          String?
  ad_content           String?
  ad_full_text_summary String?
  file_creation_date   DateTime? @db.Date
  source_file          String?
  file_path            String?
  supabase_file_path   String?
  vacancies            Int?
  posted_by            String?
  apply_within_days    Int?
  last_date_to_apply   DateTime? @db.Date
  is_active            Float?

  @@map("Ads")
}

/// Enum for standardized employment terms
enum EmploymentType {
  Permanent
  Contract
}
